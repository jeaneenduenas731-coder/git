# 问题与风险

## 当前问题

- 用户要求灯效模式一的灯珠颜色全部改成红色，已按最小范围修改 `LedEffect1()` 的点亮颜色逻辑。

## 已知风险

- 当前修改未在实机观察实际视觉效果，需要在设备上确认模式一 LED_DATA_3/LED_DATA_2 都符合预期红色显示。
- 当前环境未发现完整交叉编译工具链，无法做固件编译验证。
- 当前工作目录不是 Git 仓库，无法通过 `git diff/status` 做版本级差异检查。

## 失败尝试

- `apply_patch` 无法读取 `app_src/new/pwm_led.c`，原因是文件包含非 UTF-8 字节。
- 首次按全文件正则替换时发现匹配到 3 段分色逻辑，保护检查阻止写入，避免误改其它模式。
- 尝试用中文注释作为范围结束标记不稳定，改为使用 ASCII 标记限定 `LedEffect1()` 范围后完成定点替换。

## 待确认

- 如果用户所说“一灯珠”特指某个单独灯珠/峰值点，而不是模式一中所有被点亮灯珠，需要进一步调整范围。

## 解决记录

- 已将 `LedEffect1()` 中 LED_DATA_3 和 LED_DATA_2 的绿/黄/红分段显示改成统一红色显示。
